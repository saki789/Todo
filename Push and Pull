git remote rm origin
git remote add origin https://saki2024@github.com/saki2024/AMS
git push --set-upstream origin main

To fetch updates from a remote repository (like GitHub), you can use the following Git command:
git fetch origin

If you want to also merge the changes into your local branch, you can use:
git pull origin main
